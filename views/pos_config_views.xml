<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- Inherits `pos.config` form view to add the new fields for the configuration for custom receipt template -->
    <record id="pos_config_view_form" model="ir.ui.view">
        <field name="name">pos.config.view.form.inherit.custom.receipts.for.pos</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//setting[@id='other_devices']" position="after">
                <setting title="Custom Order Receipts" string="Custom Order Receipts" help="Use custom receipt templates for orders.">
                    <field name="is_custom_receipt"/>
                    <div class="content-group mt16">
                        <div class="row mt16">
                            <label class="col-lg-4 o_light_label"
                                   for="receipt_template_id"/>
                            <field name="receipt_template_id"
                                   invisible="not is_custom_receipt"
                                   required="is_custom_receipt"/>
                        </div>
                    </div>
                </setting>
                <div class="content-group mt16" invisible="1">
                    <field name="template_receipt"/>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
